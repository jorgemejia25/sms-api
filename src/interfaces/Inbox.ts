export interface InboxRes {
  key: [string, string, string, string, string];
}

export interface Inbox {
  phonenumber: string;
  port: string;
  portname: string;
  message: string;
  time: string;
}
